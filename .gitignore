# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.log

# 内嵌 Node 打包产物（由 npm run bundle:node 从 src 重新生成，不入库）
/public/nodejs-project/main.js

# Capacitor / Android 构建产物
/android/app/build/
/android/build/
/android/.gradle/
/android/local.properties

# 签名密钥：绝不入库，走 GitHub Secrets（CI 里从 base64 还原到临时目录）
*.jks
*.keystore
